#include<stdio.h>
int main() {
    // This program calculates the product of two numbers
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    printf("Product: %d\n", a * b);
    return 0;
}